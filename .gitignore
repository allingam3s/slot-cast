# Abhängigkeiten
node_modules/
.pnpm-store/

# Build-Ausgaben
dist/
build/
*.tsbuildinfo

# Umgebungsvariablen (NIEMALS committen!)
.env
.env.local
.env.*.local
.env.development
.env.production

# Betriebssystem
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Temporäre Dateien
*.backup
*.tmp
/tmp/

# Uploads (lokal gespeicherte Bilder – werden separat verwaltet)
# Achtung: Logo und Cover-Bild MÜSSEN committet werden!
# Diese Zeilen nur aktivieren wenn du die Bilder explizit ausschließen willst:
# artifacts/slotcast-web/public/images/

# Creator-App (wird NICHT auf GitHub Pages deployt)
packages/creator-app/dist/
packages/creator-app/uploads/temp/

# Replit (falls aus Replit exportiert)
.replit
replit.nix
.breakpoints

# Replit-interne Ordner – nicht auf GitHub pushen
.local/
.agents/
